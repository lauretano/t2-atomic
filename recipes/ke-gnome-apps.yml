# this applies the kansei edition apps and modifications
modules:
  - type: rpm-ostree
    repos:
      - https://copr.fedorainfracloud.org/coprs/ublue-os/staging/repo/fedora-%OS_VERSION%/ublue-os-staging-fedora-%OS_VERSION%.repo

  - type: rpm-ostree
    install:
      - yaru-theme

  - type: default-flatpaks
    notify: false # Send notification after install/uninstall is finished (true/false)
    system:
      # If no repo information is specified, Flathub will be used by default
      # repo-url: https://dl.flathub.org/repo/flathub.flatpakrepo
      # repo-name: flathub
      # repo-title: "Flathub (system-wide)" # Optional; this sets the remote's user-facing name in graphical frontends like GNOME Software
      install:
        - org.gnome.baobab
        - org.gnome.Calculator
        - org.gnome.Calendar
        - org.gnome.Characters
        - org.gnome.clocks
        - org.gnome.Connections
        - org.gnome.Contacts
        - ca.desrt.dconf-editor
        - org.gnome.Evince
        - com.mattjakeman.ExtensionManager
        - org.gnome.font-viewer
        - org.gnome.Logs
        - org.gnome.Loupe
        - org.gnome.Maps
        - org.gnome.NautilusPreviewer
        - org.gnome.Snapshot
        - org.gnome.TextEditor
        - org.gnome.Weather

  - type: gnome-extensions
    install:
    - nightthemeswitcherromainvigier.fr.v75
    - tiling-assistantleleat-on-github.v47
    - blur-my-shellaunetx.v61
    - weatheroclockCleoMenezesJr.github.io.v13
    - logomenuaryan_k.v30
    - dash-to-dockmicxgx.gmail.com.v92
    - user-themegnome-shell-extensions.gcampax.github.com.v57