# rpm and flatpaks for mostly vanilla kinoite
# 2024 T2-Atomic by Chris Lauretano
modules:
  - type: rpm-ostree
    install:
      - plasma-desktop
      - plasma-workspace
      - plasma-workspace-wayland
