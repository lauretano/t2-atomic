#
# 2024 Chris Lauretano <os@cdl.sh>
#
# Fedora Atomic spins for Apple T2

name: t2-atomic-plasma-kansei
# description will be included in the image's metadata
description: Fedora Atomic Plasma (aka Kinoite) spin for Apple T2 Hardware

base-image: ghcr.io/ublue-os/kinoite-main
image-version: 40

modules:
  - type: files
    files:
      - usr: /usr # copies config/files/usr into your image's /usr.
                  # put configuration files you want in /etc/ on a booted system 
                  # in /usr/etc/ in the image. read more in files module reference.
  - from-file: common-t2-enablement.yml #installs t2 kernel, t2 grub arguments, and packages for enabling t2 hardware
  - from-file: common-apps.yml #installs apps common across all the t2 editions
  - from-file: common-apps-plasma.yml #and plasma flatpaks etc because this has both
  - from-file: common-kansei.yml
  - type: signing # this sets up the proper policy & signing files for signed images to work fully
  